name: Deploy Backend to Container Apps

# Stub: registers this workflow name for workflow_dispatch so it can be
# manually run against a feature branch's full definition via
# `gh workflow run backend-deploy.yml --ref <branch>`, without going live
# on every push to main yet. GitHub only allows dispatching a workflow
# once it exists on the default branch, regardless of the ref executed.
# The real push trigger, paths filter, and job steps land when the full
# PR (#26) merges and replaces this file.
on:
  workflow_dispatch:

jobs:
  stub:
    name: Placeholder — real logic lives on feature/backend-deploy until merged
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is a stub. Dispatch with --ref pointing at the branch carrying the full workflow."
